adminPassword: changeme123
persistence:
  enabled: true
  size: 512Mi
resources:
  requests:
    memory: 128Mi
    cpu: 50m
  limits:
    memory: 256Mi
service:
  type: NodePort
  nodePort: 30080

datasources:
  datasources.yaml:
    apiVersion: 1
    datasources:
      - name: Prometheus
        type: prometheus
        url: http://prometheus-server.monitoring.svc.cluster.local
        access: proxy
        isDefault: true
